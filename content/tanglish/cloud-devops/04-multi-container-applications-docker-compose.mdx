<ThemeCard title="Multiple containers-ஐ ஒரு team-ஆக run பண்ணலாம்" badge="DOCKER HANDBOOK 04">

Frontend, order API, database மற்றும் notification service—நான்கையும் தனித்தனியாக start பண்ணாமல் ஒரே `compose.yaml` மூலம் together run பண்ணலாம்.

</ThemeCard>

<DockerFlowVisual variant="compose" />

<DockerConceptLab concept="compose" language="ta" />

## Single container போதாத real app

Food-delivery app-ல் order API ஒரு job செய்யும்; database order data save பண்ணும். இரண்டு containers-ஐ separate commands மூலம் start பண்ணலாம். ஆனால் name, setting, volume எல்லாம் ஒவ்வொரு முறையும் type பண்ணும்போது mistake chance அதிகம்.

Docker Compose அந்த setup-ஐ `compose.yaml` என்ற one file-ல் save பண்ணும். அந்த file app team-க்கான attendance sheet + contact list மாதிரி.

## நம்ம delivery app team

<HandbookLearningVisual variant="dockerCompose" />

Browser laptop port வழியாக order API-ஐ access பண்ணும். API, `postgres:5432` என்ற service name use செய்து database-ஐ contact பண்ணும். இங்கு `postgres` தான் database container-ன் contact name.

## `compose.yaml`-ஐ story மாதிரி read பண்ணலாம்

```yaml
services:
  order-api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://app:app@postgres:5432/orders
    depends_on:
      - postgres

  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: orders
    volumes:
      - order-data:/var/lib/postgresql/data

volumes:
  order-data:
```

File-ஐ story மாதிரி read பண்ணுங்க: “order-api build பண்ணு, 8000 door open பண்ணு, database address கொடு, postgres start பண்ணு, data-க்கு volume connect பண்ணு.” Real password-ஐ இப்படிப் file-ல் எழுதக்கூடாது.

## Beginners அதிகம் miss பண்ணும் `localhost` rule

Container-க்குள் `localhost` என்றால் அந்த same container. அது மற்ற service இல்லை. அதனால் API database-ஐ `localhost:5432` மூலம் அல்ல, `postgres:5432` மூலம் access பண்ண வேண்டும்.

## Start ஆனது வேறு; ready ஆனது வேறு

`depends_on` start order மட்டும் help பண்ணும். Database process start ஆன உடனே query accept பண்ண ready ஆகாமல் இருக்கலாம். Next level-ல் health check add பண்ணலாம். Beginner checkpoint: **started** மற்றும் **ready** same இல்லை.

## Daily use-க்கு தேவையான commands

```bash
docker compose up --build
docker compose ps
docker compose logs -f order-api
docker compose exec order-api sh
docker compose down
```

`docker compose down -v` named volumes-ஐயும் delete பண்ணும். Database data-ஐ delete செய்ய வேண்டும் என்று clear-ஆ decide செய்தால் மட்டும் இதை use பண்ணுங்க.

## Easy analogy: restaurant team

<Scenario title="One order, multiple specialised stations">

Restaurant-ல் counter, kitchen, pantry மற்றும் billing எல்லாம் separate stations. Customer experience ஒன்றுதான்; but ஒவ்வொரு station-க்கும் clear name மற்றும் responsibility இருக்கும். Compose அந்த operating plan மாதிரி—எந்த services இருக்கு, எப்படி connect ஆகுது, எது first ready ஆகணும் என்பதை define பண்ணும்.

</Scenario>

## எல்லாத்தையும் restart பண்ணாதீங்க—இந்த order-ல் debug பண்ணுங்க

1. `docker compose config` மூலம் resolved configuration-ஐப் பாருங்கள்.
2. `docker compose ps` மூலம் service மற்றும் health state-ஐப் பாருங்கள்.
3. Failed request-ஐ பெற்ற service-ன் logs-ஐ முதலில் பாருங்கள்.
4. Container-to-container URL-ல் wrong-ஆ `localhost` use பண்ணியிருக்கீங்களா check பண்ணுங்க.
5. Environment variables மற்றும் database migration-ஐ verify செய்யுங்கள்.

## Common mistakes

<Mistakes>

- Another container-ஐ `localhost` மூலம் call செய்வது.
- எல்லா internal ports-ஐ host-க்கு publish செய்வது.
- Startup order-ஐ readiness guarantee என்று நினைப்பது.
- Production secrets-ஐ `compose.yaml`-ல் எழுதுவது.
- `docker compose down -v` effect புரியாமல் database volume delete செய்வது.

</Mistakes>

## Interview answer—Compose networking

<ThemeCard title="Docker Compose-ல் services எப்படி communicate செய்கின்றன?" badge="CAREER">

Compose app-க்கு private network create பண்ணும். அந்த network-ல் service name contact name மாதிரி work ஆகும். அதனால் API, database-ஐ `localhost` என்று அல்ல `postgres` என்று call பண்ணும். User access செய்ய வேண்டிய service-க்கு மட்டும் host port publish பண்ணுவேன்.

</ThemeCard>

## Byte 04 quick recap

<Takeaways>

- Compose multiple containers setup-ஐ one readable file-ல் save பண்ணும்.
- `services` section app team members list.
- Service name container-to-container contact name.
- Another container-ஐ call பண்ண `localhost` use பண்ணக்கூடாது.
- Database data-க்கு named volume connect பண்ண வேண்டும்.

</Takeaways>

## Official references

- [Docker Docs: Compose quickstart](https://docs.docker.com/compose/gettingstarted/)
- [Docker Docs: Compose application model](https://docs.docker.com/compose/intro/compose-application-model/)
- [Docker Docs: Startup order](https://docs.docker.com/compose/how-tos/startup-order/)
