<ThemeCard title="Local AI demo-வை production-க்கு ready பண்ணலாம்" badge="DOCKER HANDBOOK 05">

LLM-powered customer-support API local-ல் work ஆகுது. இப்போ API key leak ஆகாமல், cost control miss ஆகாமல், problem வந்தால் rollback செய்யக்கூடிய விதத்தில் deploy பண்ணுவது எப்படி என்று பார்ப்போம்.

</ThemeCard>

<DockerFlowVisual variant="aiDeploy" />

<DockerConceptLab concept="deploy" language="ta" />

## Image-க்குள் என்ன இருக்கணும்? Outside என்ன இருக்கணும்?

Food-delivery support API user question-ஐ validate பண்ணும், approved order context-ஐ retrieve பண்ணும், LLM provider-ஐ call பண்ணும், final answer return பண்ணும். App code மற்றும் dependencies image-க்குள் இருக்கலாம். API key மற்றும் environment-specific values runtime-ல் outside-லிருந்து வரணும்.

```python
import os
from fastapi import FastAPI, HTTPException

app = FastAPI()

@app.get("/health")
def health():
    return {"status": "healthy"}

@app.post("/support")
def support(question: str):
    if not os.getenv("LLM_API_KEY"):
        raise HTTPException(500, "LLM provider is not configured")
    return {"answer": f"Demo response for: {question}"}
```

## Safer production image build பண்ணலாம்

```dockerfile
FROM python:3.12-slim
WORKDIR /app
RUN addgroup --system app && adduser --system --ingroup app app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY --chown=app:app app.py .
USER app
EXPOSE 8000
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

Small trusted base image, dependency layer caching மற்றும் non-root user நல்ல defaults. Tested versions-ஐ pin செய்து final image-ஐ scan பண்ண வேண்டும்.

## API key image-க்குள் போகவே கூடாது

`ENV LLM_API_KEY=...` அல்லது `COPY .env .` image history-ல் secret leak செய்யலாம். Local-ல் runtime environment variable; production-ல் platform secret manager மற்றும் least-privilege access use செய்யுங்கள்.

## User question வந்ததும் என்ன நடக்குது?

<HandbookLearningVisual variant="dockerAi" />

User request HTTPS ingress வழியாக API container-க்கு வரும். Container secret manager, order database மற்றும் LLM provider-ஐ controlled access மூலம் use செய்யும். Logs, metrics மற்றும் traces request outcome-ஐ record செய்யும்.

## Peak-time scenario: “என் order எங்கே?”

<Scenario title="Demo start ஆகுவது மட்டும் success இல்லை">

Hundreds of customers “என் order எங்கே?” என்று same time-ல் கேட்கலாம். Provider slow ஆகலாம்; user very long input அனுப்பலாம்; unlimited retry loop cost-ஐ increase பண்ணலாம். Team input limit, timeout, concurrency limit, token budget மற்றும் rollback policy set பண்ணும். Container running-ஆ இருக்கிறதா மட்டும் பார்க்காமல் answer safe-ஆ, right time-ல், controlled cost-ல் கிடைக்கிறதா measure பண்ணும்.

</Scenario>

## App alive-ஆ? Traffic handle பண்ண ready-ஆ?

| Check | அது சொல்லும் விஷயம் |
|---|---|
| Liveness | Process உயிருடன் இருக்கிறதா? |
| Readiness | New traffic receive செய்யலாமா? |
| Evaluation | Answer expected quality meet ஆகிறதா? |
| Cost metric | Request ஒன்றுக்கு tokens/cost எவ்வளவு? |

Paid LLM call-ஐ frequent liveness probe-ஆக use பண்ணாதீங்க.

## Deploy button click பண்ணும் முன்

1. Image-க்கு immutable version tag கொடுங்கள்.
2. Dependencies மற்றும் image vulnerabilities scan செய்யுங்கள்.
3. Non-root process confirm செய்யுங்கள்.
4. Secrets runtime-ல் inject செய்யுங்கள்.
5. CPU, memory, concurrency, timeout limits set செய்யுங்கள்.
6. Quality, latency, errors மற்றும் cost observe செய்யுங்கள்.
7. Limited traffic-க்கு release செய்து rollback path-ஐ ready-ஆக வைத்திருங்கள்.

## Problem வந்தால் எங்கே start பண்ணுவது?

| Symptom | First check பண்ண வேண்டிய evidence | Possible reason |
|---|---|---|
| Container மீண்டும் மீண்டும் restart ஆகிறது | Platform events மற்றும் startup logs | Missing variable, wrong command அல்லது memory limit |
| Request timeout ஆகிறது | Dependency latency trace | Provider delay, network அல்லது timeout mismatch |
| Provider `401` return செய்கிறது | Safe error category மற்றும் secret version | Invalid, expired அல்லது unauthorized key |
| Local-ல் works; cloud-ல் fail | Bind address, port மற்றும் platform contract | Wrong port அல்லது `localhost` binding |
| Cost திடீரென உயர்கிறது | Request count, tokens மற்றும் retry metrics | Unbounded input, retry storm அல்லது abuse |
| Release-க்குப் பிறகு answer quality குறைகிறது | Evaluation results by version | Prompt, model, retrieval அல்லது data change |

## Common mistakes

<Mistakes>

- Provider key-ஐ Dockerfile அல்லது image context-ல் copy செய்வது.
- Every provider error-ஐ retry செய்து cost storm create பண்ணுவது.
- `latest` tag மட்டும் deploy செய்து rollback version தெரியாமல் போவது.
- Full private prompts-ஐ logs-ல் store செய்வது.
- Infrastructure uptime மட்டும் பார்த்து answer quality ignore செய்வது.

</Mistakes>

## Interview answer—safe AI deployment

<ThemeCard title="LLM application-ஐ எப்படி safely deploy செய்வீர்கள்?" badge="CAREER">

Secrets இல்லாத minimal, versioned image உருவாக்கி non-root user-ஆக run செய்வேன். Deployment platform secret manager மூலம் provider key inject செய்யும். Liveness-ஐ costly model evaluation-லிருந்து separate செய்வேன். Resource, concurrency மற்றும் timeout limits அமைத்து latency, errors, quality மற்றும் cost-ஐ observe செய்வேன். Limited traffic release மற்றும் tested rollback path வைத்திருப்பேன்.

</ThemeCard>

## Docker Handbook final recap

<Takeaways>

- Image-ல் code மற்றும் dependencies; secret runtime-ல் மட்டும்.
- Healthy container useful AI answer-ஐ guarantee பண்ணாது.
- Quality, latency, safety மற்றும் cost ஒன்றாக monitor செய்ய வேண்டும்.
- Immutable version rollout மற்றும் rollback-ஐ clear ஆக்கும்.
- Platform controls container-ஐ production system-ஆக complete செய்யும்.

</Takeaways>

## Official references

- [Docker Docs: Build best practices](https://docs.docker.com/build/building/best-practices/)
- [Docker Docs: Build secrets](https://docs.docker.com/build/building/secrets/)
- [Docker Docs: Run containers](https://docs.docker.com/engine/containers/run/)
